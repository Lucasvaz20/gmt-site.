<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GMT Treinamento Profissional</title>
  <link href="https://fonts.googleapis.com/css2?family=Oxanium:wght@300;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Oxanium', sans-serif;
      margin: 0;
      background-color: #1c1c3c;
      color: #fff;
    }
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
      background-color: #001f3f;
      position: sticky;
      top: 0;
      z-index: 1000;
    }
    .logo {
      font-size: 2rem;
      font-weight: 600;
      color: #ffc107;
      text-decoration: none;
      transition: transform 0.3s;
    }
    .logo:hover {
      transform: scale(1.1);
    }
    nav a {
      margin: 0 1rem;
      color: #ffffff;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s;
    }
    nav a:hover {
      color: #ffc107;
    }
    main {
      padding: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }
    section {
      margin-bottom: 3rem;
      background: #2c2c54;
      padding: 1.5rem;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0,0,0,0.3);
      animation: fadeIn 1s ease-in-out;
    }
    .training-list {
      list-style: none;
      padding: 0;
    }
    .training-list li {
      margin: 0.5rem 0;
    }
    .game-image {
      max-width: 100%;
      border-radius: 8px;
      margin-top: 1rem;
    }
    .audio-practice {
      margin-top: 1rem;
    }
    .button {
      background-color: #ffc107;
      border: none;
      padding: 0.5rem 1rem;
      cursor: pointer;
      border-radius: 5px;
      font-weight: bold;
      transition: background-color 0.3s;
    }
    .button:hover {
      background-color: #e0a800;
    }
    .progress-bar {
      width: 100%;
      background: #ccc;
      height: 10px;
      margin-top: 1rem;
      border-radius: 5px;
    }
    .progress {
      height: 10px;
      background: #ffc107;
      width: 0%;
      border-radius: 5px;
      transition: width 0.3s ease;
    }
    footer {
      text-align: center;
      padding: 1rem;
      background-color: #001f3f;
      color: #ccc;
      font-size: 0.9rem;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @media (max-width: 768px) {
      header, nav {
        flex-direction: column;
        text-align: center;
      }
      nav a {
        margin: 0.5rem 0;
      }
    }
  </style>
  <script>
    function updateProgress() {
      const checkboxes = document.querySelectorAll('input[type="checkbox"]');
      const checked = [...checkboxes].filter(ch => ch.checked).length;
      const percent = (checked / checkboxes.length) * 100;
      document.getElementById('progress').style.width = percent + '%';
    }
    function enviarAudio() {
      document.getElementById('audio-feedback').textContent = "Áudio enviado com sucesso!";
    }
  </script>
</head>
<body>
  <header>
    <a class="logo" href="#">GMT</a>
    <nav>
      <a href="#fortnite">Fortnite</a>
      <a href="#apex">Apex</a>
      <a href="#valorant">Valorant</a>
      <a href="#cod">Warzone</a>
      <a href="#cs2">CS2</a>
      <a href="#ingles">Inglês</a>
    </nav>
  </header>
  <main>
    <!-- Seções dos jogos e inglês mantidas -->
    <!-- Conteúdo completo já presente e mantido -->
    <!-- Ver conteúdo do documento atual para os blocos de jogo e inglês -->
  </main>
  <footer>
    &copy; 2025 GMT Treinamento Profissional. Todos os direitos reservados.
  </footer>
</body>
</html>
